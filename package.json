{
  "name": "pulsar-runner",
  "version": "1.0.0",
  "description": "",
  "main": "./src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "atom": "<2.0.0"
  },
  "author": "confused-Techie",
  "repository": "https://github.com/confused-Techie/pulsar-runner",
  "license": "MIT",
  "configSchema": {
    "commandTimeout": {
      "type": "integer",
      "default": 300000,
      "description": "The timeout for running commands in milliseconds."
    }
  },
  "dependencies": {
    "uuid": "^9.0.1"
  }
}
